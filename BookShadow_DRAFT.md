# 书影（BookShadow）

家庭藏书管理桌面应用，支持书籍信息管理、Markdown 书评。

## 功能

1. 书籍管理：支持新增、编辑、删除，支持网格 / 列表两种视图浏览，支持按星级（1-5 星）、地域（中国、日本、美国等）、类别（小说、历史等）、语言（中文、英语等）筛选
2. 信息获取：支持通过 ISBN 从 豆瓣 / Google Books / Open Library 等平台（可扩展）获取图书元数据
3. 书评系统：基于内置 Markdown 编辑器进行书评，记录书评日期，可从 `.md` 文件导入
4. 全文搜索：按书名、作者、简介、标签模糊搜索，可支持

## 技术栈

整体采用 Tauri + Rust + React 框架。
数据存储采用 SQLite，存放在 `~/.bookshadow/sqlite` 文件夹下。


## 存储目录

以 `~/.bookshadow/` 作为程序相关数据的存放位置
