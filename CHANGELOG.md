# CHANGELOG

## v0.6.0（2026-05-10）

**数据质量完善**

- 新增「开始阅读」`started_at`、「完成阅读」`finished_at`、「丛书」`series` 三个字段
- 豆瓣抓取自动解析「丛书」字段
- 阅读状态与日期联动：切「在读」自动填入今日为开始日期，切「已读」自动填入今日为完成日期，切「想读」清空两个日期；取消状态时清对应日期；所有日期均可手动修改
- 日期输入支持文字直接输入（格式 YYYY-MM-DD）与日历控件双通道
- 星级评分移至封面下方
- 编辑界面隐藏书评区，减少视觉干扰
- 数据完整度提示：地域、类别、语言任一缺失时在编辑入口旁显示「待完善」
- 筛选面板新增「未设」状态选项，可过滤尚未设置阅读状态的书籍
- 搜索覆盖范围扩展至出版社和丛书名
- 编辑书籍后立即刷新当前过滤结果，避免列表与筛选条件不一致
- 移除 FTS5 全文索引（中文两字词命中率低，小型库无性能优势），改为纯 LIKE 方案
- 清理死代码（`search.rs` 及相关 FTS5 触发器/虚拟表）

---

## v0.5.7（2026-05-10）

**筛选计数后端化**

- 筛选面板各维度计数改由后端 SQL `GROUP BY` 聚合（新增 `get_filter_counts` 命令），搜索词始终参与计数，每个维度排除自身过滤条件
- 标签补全数据源改用全库聚合，覆盖面从分页 40 条扩展至全库
- 删除前端 `allBooks` / `refreshAllBooks`，状态管理简化
- 标签筛选改为可搜索下拉选择器（`SearchableSelect`）
- 键盘快捷键修复：焦点在排序下拉框时 `/` 现可正确聚焦搜索框

---

## v0.5.6（2026-05-06）

**筛选与查询问题修复**

- 修复地域/类别筛选条件无效的问题
- 修复搜索词在翻页后丢失的问题
- 地域、类别筛选新增可搜索下拉，支持超出主列表的选项

---

## v0.5.4（2026-05-03）

**本地封面上传**

- 支持从本地文件上传书籍封面（替换自动下载的远程封面）
- 手动上传后清空 `cover_url`，防止下次保存时被远程封面覆盖
- 封面响应添加 `Cache-Control: no-store` + `coverNonce`，确保同文件名更新后 WebKit 不读缓存

---

## v0.5.2（2026-04-29）

**细节优化**

- 豆瓣抓取兼容性改进（nbsp 处理、独立冒号 token 合并）
- 筛选面板地域/类别支持折叠式「其他」分组
- 工具栏增加网格/列表视图切换
- CodeMirror 独立分包，优化首屏加载体积

---

## v0.5.0（2026-04-26）

**导出与标签**

- 书籍数据导出为 CSV
- 标签支持：多标签打标、按标签筛选，标签输入框支持回车/逗号分隔
- 设置页面：配置豆瓣 Cookie 与 Google Books API Key
- 筛选面板新增标签、语言、年代维度

---

## v0.4.0（2026-04-26）

**书籍详情重构**

- BookDetail 改为内联编辑（原为独立弹层），封面在编辑时始终可见
- 支持通过豆瓣 subject URL 直接抓取书籍信息（解决同 ISBN 多版本问题）
- 批量导入流程优化：扫码并行，网络抓取串行（避免豆瓣频率限制）
- 星级评分组件独立，支持点击同一星级取消评分

---

## v0.3.0（2026-04-25）

**批量 ISBN 导入**

- 支持拖拽或选择图片批量扫描条码（EAN-13），纯 Rust 实现（`rxing`），无原生依赖
- 扫描结果预览，可逐条确认后批量写入数据库
- 设置页面雏形（豆瓣 Cookie 配置）

---

## v0.2.2（2026-04-24）

**初始版本**

- 书籍 CRUD（标题、作者、ISBN、出版社、出版日期、语言、地域、类别、评分、简介、译者）
- ISBN 元数据自动获取：豆瓣 → Google Books → Open Library 级联抓取
- 封面自动下载，`bookcover://` 自定义协议本地服务
- Markdown 书评编辑（CodeMirror 6，语法高亮）
- 多维度筛选面板（状态、星级、地域、类别、语言、年代）
- 网格/列表双视图，分页加载（每页 40 条）
- 全文搜索（标题、作者、译者、出版社、简介）
- macOS 原生标题栏（`TitleBarStyle: Overlay`），窗口拖拽区域
- 数据存储于 `~/.bookshadow/`（SQLite + 本地封面缓存）
